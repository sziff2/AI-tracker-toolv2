# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Task queue (Celery-ready, not yet deployed as separate worker)
celery==5.4.0
redis==5.1.0

# Document parsing
pymupdf==1.24.10
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5
beautifulsoup4==4.12.3
lxml==5.3.0

# LLM
anthropic==0.34.0

# Semantic search (Tier 3.4 — pgvector + local embeddings)
# pgvector:               Python bindings for the Postgres vector extension
# sentence-transformers:  runs BAAI/bge-small-en-v1.5 in-process (384 dims).
#                         No per-token cost, no third-party API, data stays on
#                         the box. Quality on financial-text retrieval is
#                         within a few pp of OpenAI text-embedding-3-small.
# torch (CPU-only wheel): sentence-transformers' inference backend. The +cpu
#                         pin avoids pulling the ~2GB CUDA wheel — Railway
#                         containers are CPU-only and the full wheel would
#                         multiply image size 4x.
--extra-index-url https://download.pytorch.org/whl/cpu
pgvector==0.3.6
torch==2.5.0+cpu
sentence-transformers==3.3.1

# Web scraping
cloudscraper==1.2.71

# Rate limiting
slowapi==0.1.9

# Utilities
tenacity==8.2.3
python-multipart==0.0.9
httpx==0.27.2
aiofiles==24.1.0
python-dateutil==2.9.0
jinja2==3.1.4

# PDF rendering (Tier 3.3 — briefing download). Pure Python, no native
# deps — deliberate choice over WeasyPrint to avoid Cairo/Pango on the
# Railway image. Roadmap allows swapping to WeasyPrint later if CSS
# polish matters.
reportlab==4.4.10

# Quantitative (optimisation + factor regressions)
# OLS regressions for factor shocks are pure-numpy — no statsmodels needed.
numpy==1.26.4
scipy==1.13.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
